---
'everquint-backend-skill': patch
---

Replace the TypeScript-oriented `src/api` layout with language-aware sibling modules under `src`, root-level debug and documentation directories, and generated TypeScript output under `dist`.
